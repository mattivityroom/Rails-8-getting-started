<h1>Products</h1>

<%= link_to("New Product", new_product_path) %>

<hr>

<table>
    <thead>
        <tr>
            <th>Name</th>
        </tr>
    </thead>

    <tbody>
        <% @products.each do |product| %>
            <tr>
                <td>
                    <%= link_to(product.name, product) %>
                </td>
            </tr>
        <% end %>
    </tbody>
</table>